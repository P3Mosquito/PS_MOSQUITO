import 'package:flutter/material.dart';

class ZoneMapScreen extends StatelessWidget {
  const ZoneMapScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Mapa de Zona');
  }
}
