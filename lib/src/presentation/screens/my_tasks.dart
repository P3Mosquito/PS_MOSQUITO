import 'package:flutter/material.dart';

class TasksMobile extends StatelessWidget {
  const TasksMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tasks Mobile'),
      ),
    );
  }
}
